import os.path

BASE_PATH = os.path.dirname(os.path.abspath(__file__))



if __name__ == "__main__":
    # lines =[" "," "," nzh ",None]
    # point_cloud_names = [line.strip() + "2%" for line in lines]
    # point_cloud_names = list(filter(None, point_cloud_names))
    # print(" ".strip() == "")
    # rng=np.random.RandomState(123)
    # for _ in range(50):
    #     print(iter(rng.choice(120, size=100, replace=False)))

    # delete()
    print(233)

    # arr = np.array([[0, 0],[1, 0]])
    # arr2=np.array([5,4,3])
    # print(arr2[arr])
    # outliers = np.array(arr,dtype=float)
    # indexes = np.array(range(5))
    # print(outliers == 0)
    # inlier_idx = indexes[outliers == 0]
    #
    # outlier_idx = indexes[outliers == 1]
    # print(inlier_idx)
    # rng=np.random.RandomState(1478)
    # print(rng.uniform(0.28))
    #
    # for i in range(0,10):
    #     print("lala")
    #
    # arr = np.array([0, 1])
    # buf=[1,4,5,7]
    # print(buf[arr])
    # buf = np.array([[1, 2], [4, 5]])
    # print(np.concatenate((arr, buf), axis=0))

    # arr0=0
    # arr1=np.array([1,3])
    # arr2=np.array([4,5])
    #
    # print(np.concatenate((arr0,arr1,arr2),axis=0))
    # print(arr.mean(0))

    # arr=torch.tensor([1])
    # print(arr)
    # print(i)
